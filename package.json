{
  "name": "tri-state-zipline-rental",
  "version": "1.0.0",
  "private": true,
  "description": "Node.js production-style website for Tri-State Zipline Rental.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check src/data.js && node --check src/render.js && node --check public/app.js && node --check scripts/export-static.js",
    "pages:build": "node scripts/export-static.js"
  },
  "engines": {
    "node": ">=20"
  }
}
